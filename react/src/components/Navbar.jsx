function Navbar() {
    return (
        <nav className="navbar">
            <a href="#">Dashboard</a>
            <a href="#">History</a>
            <a href="#">Stats</a>
            <a href="#">Settings</a>
        </nav>
    );
}
export default Navbar;